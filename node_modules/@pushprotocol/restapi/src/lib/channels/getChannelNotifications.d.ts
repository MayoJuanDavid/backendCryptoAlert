import { ENV } from '../constants';
import { NotifictaionType } from '../types';
type GetChannelOptionsType = {
    channel: string;
    env?: ENV;
    page?: number;
    limit?: number;
    filter?: NotifictaionType | null;
    raw?: boolean;
};
export declare const getChannelNotifications: (options: GetChannelOptionsType) => Promise<any>;
export {};
