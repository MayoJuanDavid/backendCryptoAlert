import { ENV } from '../constants';
/**
 *  GET /v1/channels/{addressinCAIP}
 */
type getChannelsOptionsType = {
    env?: ENV;
    page?: number;
    limit?: number;
    sort?: string;
    order?: string;
    filter?: number;
    tag?: string;
    user_subscribed?: string;
};
export declare const getChannels: (options: getChannelsOptionsType) => Promise<any>;
export {};
