import { ENV } from '../constants';
/**
 * Represents the response type for the chat status.
 */
export interface ChatInfoResponse {
    meta: {
        group: boolean;
        encrypted: boolean;
        visibility: boolean;
        groupInfo: {
            public: boolean;
        };
    };
    list: string;
    participants: string[];
    chatId: string;
    recipient: string;
}
/**
 * Represents the input type for fetching chat status.
 */
export interface GetChatInfoType {
    recipient: string;
    account: string;
    env?: ENV;
}
/**
 * Fetches the chat status for a given chat ID and address.
 */
export declare const getChatInfo: (options: GetChatInfoType) => Promise<ChatInfoResponse>;
