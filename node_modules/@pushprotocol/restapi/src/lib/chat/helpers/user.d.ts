import { ENV } from '../../constants';
import { IConnectedUser, walletType } from '../../types';
import { IPGPHelper } from '.';
export declare const getConnectedUserV2: (wallet: walletType, privateKey: string | null, env: ENV) => Promise<IConnectedUser>;
export declare const getConnectedUserV2Core: (wallet: walletType, privateKey: string | null, env: ENV, pgpHelper: IPGPHelper) => Promise<IConnectedUser>;
