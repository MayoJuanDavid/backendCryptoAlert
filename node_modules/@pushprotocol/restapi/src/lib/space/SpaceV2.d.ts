import { SpaceInfoDTO } from '../types';
import Space from './Space';
import { ChatUpdateSpaceType } from './update';
export declare class SpaceV2 {
    private spaceV1Instance;
    private spaceInfo;
    constructor({ spaceV1Instance, spaceInfo, }: {
        spaceV1Instance: Space;
        spaceInfo: SpaceInfoDTO;
    });
    start(): Promise<void>;
    join(): Promise<void>;
    update(updateSpaceOptions: ChatUpdateSpaceType): Promise<void>;
    leave(): Promise<void>;
    stop(): Promise<void>;
    requestForMic(): Promise<void>;
    acceptMicRequest({ address, signal, }: {
        address: string;
        signal: any;
    }): Promise<void>;
    rejectMicRequest({ address }: {
        address: string;
    }): Promise<void>;
    inviteToPromote({ address }: {
        address: string;
    }): Promise<void>;
    acceptPromotionInvite({ signal }: {
        signal: any;
    }): Promise<void>;
    rejectPromotionInvite(): Promise<void>;
    config({ audio }: {
        audio?: boolean;
    }): void;
}
