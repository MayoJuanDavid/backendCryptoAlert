import { HttpStatus } from "./httpStatus";
export declare class ValidationError extends Error {
    status: HttpStatus;
    errorCode: string;
    details: string;
    constructor(status: HttpStatus, errorCode: string, message: string, details: string);
    format(): object;
}
export declare function isErrorWithResponse(error: unknown): error is {
    response: {
        status: number;
        data: {
            status: number;
            errorCode: string;
            message: string;
            details: string;
            timestamp: string;
        };
    };
};
export declare function isErrorWithResponseV2(error: unknown): error is {
    response: {
        status: number;
        data: {
            error: string;
            message: string;
            validation: string;
        };
    };
};
export declare function handleError(error: unknown, context: string): ValidationError;
